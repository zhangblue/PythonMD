# 变量
> python是以数据为主，当使用`a=1`和`a=2`时，并不是改变a的引用。

- 相同的变量名使用的是不同的地址:

```python
a=1
id(a)
a=2
id(a)
#此时两次打印的a的内存地址是不同的
```

- 不同的变量名使用相同的地址

```python
a=123
b=123
id(a)
id(b)
# 此时得到的a的内存地址与b的内存地址相同。
```
